/** @type {import('next').NextConfig} */
const nextConfig = {
   // output: 'export',
   //  distDir: 'dist',
   images: {
      //   unoptimized: true,
      domains: [

         "images.pexels.com",
         "plast.org.ua",
      ]
   },
}

module.exports = nextConfig
